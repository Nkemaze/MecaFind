import { useState } from 'react';
import api from '../services/api';

export default function ChangePassword() {
  const [currentPassword, setCurrentPassword] = useState(''); const [newPassword, setNewPassword] = useState(''); const [confirm, setConfirm] = useState(''); const [message, setMessage] = useState('');
  async function submit(event) { event.preventDefault(); if (newPassword !== confirm) return setMessage('New passwords do not match.'); try { const { data } = await api.post('/auth/change-password', { currentPassword, newPassword }); setMessage(data.message); setCurrentPassword(''); setNewPassword(''); setConfirm(''); } catch (error) { setMessage(error.response?.data?.message || 'Could not change password.'); } }
  return <main className="pt-20 min-h-screen bg-background flex items-center justify-center px-5"><div className="w-full max-w-md bg-white rounded-2xl border border-outline-variant/30 p-8 shadow-sm"><h1 className="text-2xl font-bold text-on-surface mb-6">Change password</h1><form onSubmit={submit} className="space-y-4"><input type="password" required value={currentPassword} onChange={(event) => setCurrentPassword(event.target.value)} placeholder="Current password" className="w-full h-12 px-4 rounded-lg bg-surface border border-outline-variant/60 focus:border-primary focus:outline-none" /><input type="password" required minLength={8} value={newPassword} onChange={(event) => setNewPassword(event.target.value)} placeholder="New password" className="w-full h-12 px-4 rounded-lg bg-surface border border-outline-variant/60 focus:border-primary focus:outline-none" /><input type="password" required minLength={8} value={confirm} onChange={(event) => setConfirm(event.target.value)} placeholder="Confirm new password" className="w-full h-12 px-4 rounded-lg bg-surface border border-outline-variant/60 focus:border-primary focus:outline-none" /><button className="w-full h-12 rounded-lg bg-primary text-on-primary font-semibold">Change password</button></form>{message && <p className="mt-5 text-sm text-on-surface-variant">{message}</p>}</div></main>;
}
